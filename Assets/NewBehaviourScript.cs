using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class NewBehaviourScript : MonoBehaviour
{
    private Transform TPpoint;
    public Transform Transform;
    public  Vector3 EndTP;

    // Start is called before the first frame update
    void Start()
    {
        //TPpoint = GameObject.FindGameObjectWithTag("TPpoint").transform;
        //Vector3 EndTP = TPpoint.position;
        //EndTP.y = transform.position.y;
        //EndTP.x = transform.position.x;
        //EndTP.z = transform.position.z;
       
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
